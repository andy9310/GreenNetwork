[project]
name = "green_network"
version = "0.1.0"
description = "research code , Chun-an Yang 2025/12/03"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "networkx>=3.6",
  "numpy",
  "scikit-learn>=1.7.2",
  "torch",
]
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
